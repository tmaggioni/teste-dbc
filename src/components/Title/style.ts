import styled from 'styled-components'

export const TitleStyle = styled.h1`
    font-family: var(--exo);
    font-style: normal;
    font-weight: bold;
    font-size: 28px;
    line-height: 24px;
    color:#505050;
`